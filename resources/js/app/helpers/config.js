const appName = 'Base Admin';
const apiBaseUrl = 'http://baseadmin.test/api/0/';
const loadSettings = {
    color: '#4143cc',
    loader: 'dots', //spinner
    width: 64,
    height: 64,
    backgroundColor: '#ffffff',
    opacity: 0.5,
    zIndex: 999,
};

export { appName, apiBaseUrl, loadSettings }